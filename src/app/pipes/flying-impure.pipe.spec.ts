import { FlyingImpurePipe } from './flying-impure.pipe';

describe('FlyingImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyingImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
